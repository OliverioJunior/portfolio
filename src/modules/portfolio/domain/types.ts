export interface Project {
  title: string;
  description: string;
  image: string;
  tags: string[];
  link: string;
}

export interface Profile {
  name: string;
  title: string;
  description: string;
}
